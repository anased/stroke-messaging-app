import { clsx } from "clsx"
import { twMerge } from "tailwind-merge"
 
export function cn(...inputs) {
  return twMerge(clsx(inputs))
}

export { Button, Card, CardHeader, CardTitle, CardContent, Input, Label, Textarea }